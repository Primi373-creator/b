{
  "name": "apialpha",
  "description": "apialpha: just some API",
  "dependencies": {
    "axios": "^1.6.7",
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "pm2": "^5.1.2"
  },
  "scripts": {
    "start": "pm2 start index.js --deep-monitoring --attach --name apialpha",
    "stop": "pm2 stop apialpha"
  }
}
